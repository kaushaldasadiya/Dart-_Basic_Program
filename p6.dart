import 'dart:io';
void main()
{
 
  for(int i=1;i<=3;i++)
  { print("Enter number$i:");
    int no= int.parse(stdin. readLineSync()!);
    int square= no*no;
    int cube = no*no*no; 
    print("$no square is $square");
    print("$no square is $cube");
    print("______________");

    }
   
  }
  
  
